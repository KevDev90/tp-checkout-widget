# Copy to .env for local Express proxy (npm run server). Never commit .env.
# Production worker: set via `wrangler secret put TRUSTPILOT_API_KEY`.

TRUSTPILOT_API_KEY=

# Optional — Express proxy only (server.js)
# PORT=3000
# CACHE_TTL_MS=2700000
# IMAGE_CACHE_TTL_MS=3600000
